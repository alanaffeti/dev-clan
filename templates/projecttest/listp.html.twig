
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <img src="{{ asset('/imagesfront/logo.png') }}" >
    <title>Details sur reparation voiture</title>
    <style>
        body {
            background-color: #f8f8f8;
            font-family: Arial, sans-serif;
        }
        .container {
            margin: 0 auto;
            max-width: 100%;
            padding: 50px;
            background-color: #fff;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.2);
        }
        h1 {
            margin-top: 0;
            font-size: 36px;
            color: #333;
            text-align: center;
        }
        table {
            border-collapse: collapse;
            width: 100%;
            margin-top: 30px;
        }
        th, td {
            text-align: left;
            padding: 10px 15px;
            border: 1px solid #ddd;
            font-size: 10px;
        }
        th {
            background-color: #4CAF50;
            color: white;
        }
       /* img {
            max-width: 100%;
            height: auto;
            display: block;
            margin: 0 auto;
            margin-top: 50px;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>Details sur reparation de voiture </h1>
    <table>
        <thead>
        <tr>
            <th>Identifiant</th>
            <th>Nom</th>
            <th>Prenom</th>
            <th>Adresse mail</th>
            <th>numero_telephone</th>
            <th>lieu_garage</th>

            <th>Description</th>


        </tr>
        </thead>
        <tbody>

            <tr>
                <td>{{project.id}}</td>
                <td>{{project.nom}}</td>
                <td>{{project.prenom}}</td>
                <td>{{project.adressemail}}</td>
                <td class="text-right">{{project.numtel}}</td>
                <td class="text-right">{{project.lieugarage}}</td>
                   <td>{{ facture.description }}</td>



            </tr>

        </tbody>
    </table>

</div>
</body>
</html>
