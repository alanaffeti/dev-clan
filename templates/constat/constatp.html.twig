 ./templates/default/mypdf.html.twig 
<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
  {# <img src="{{ asset('public/asset1/LOGO.png') }}" alt="logo"> #}
	<title>Constat</title>
	<style>
		body {
			background-color: #f8f8f8;
			font-family: Arial, sans-serif;
		}
		.container {
			margin: 0 auto;
			max-width: 800px;
			padding: 50px;
			background-color: #fff;
			box-shadow: 0px 0px 10px rgba(0,0,0,0.2);
		}
		h1 {
			margin-top: 0;
			font-size: 36px;
			color: #333;
			text-align: center;
		}
		table {
			border-collapse: collapse;
			width: 100%;
			margin-top: 30px;
		}
		th, td {
			text-align: left;
			padding: 10px 15px;
			border: 1px solid #ddd;
			font-size: 16px;
		}
		th {
			background-color: #4CAF50;
			color: white;
		}
		img {
			max-width: 100%;
			height: auto;
			display: block;
			margin: 0 auto;
			margin-top: 50px;
		}
	</style>
</head>
<body>
    {# <h1>Constat</h1> #}
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ constat.id }}</td>
            </tr>
            
            <tr>
                <th>Id de client</th>
                <td>{{ constat.idClient }}</td>
            </tr>
            <tr>
                <th>Id de vehicule</th>
                <td>{{ constat.idVehicule }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ constat.date ? constat.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ constat.lieu }}</td>
            </tr>
            <tr>
              {# <th>Image</th> #}
              {# {% set filePaths = constat.image| split(',') %}
{% for filePath in filePaths %}
    <tr>
        <th>Image</th>
        <td>
            <div style="width: 200px; height: 200px;">
                <img src="{{ asset('uploads/add/' ~ filePath) }}" style="max-width: 100%; max-height: 100%; object-fit: contain;">
            </div>
        </td>
    </tr> #}
{# {% endfor %}
             #}
            </tr>
            <tr>
             <tr>

        </tbody>
    </table>

    </body>
</html>

