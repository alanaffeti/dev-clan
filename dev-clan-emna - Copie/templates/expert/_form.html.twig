{{ form_start(form,{'attr' : {'novalidate' : 'novalidate'}} ) }}

    {{ form_widget(form) }}
    {{ form_errors(form.cin,{'attr':{'class':'text-danger'}}) }}
   <center><button class="btn btn-success">{{ button_label|default('Save') }}</button></center>6
{{ form_end(form) }}

